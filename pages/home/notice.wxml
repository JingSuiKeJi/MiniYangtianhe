<view class="container">
 <!-- <navBar title='{{title}}' background='white' color='black' iconTheme='black' back="{{true}}" class="ui-navBar"></navBar> -->
	<view class="ui-main">
	    <web-view src="{{url}}" class="ui-webView"></web-view>
	</view>
</view>