<!--pages/step/illustration.wxml-->
<navBar title='福气说明' background='linear-gradient(137deg,rgba(1,206,165,1) 0%,rgba(94,221,218,1) 100%)' color='white' iconTheme='white' back="{{true}}"></navBar>
<view class="ui-main">
    <view class="ui-content" wx:for="{{rules}}" wx:key="{{index}}">
        <view class="ui-content_title">Q：{{item.title}}</view>
        <view class="ui-content_answer"><text >A：</text>{{item.content}}</view>
    </view>
</view>
